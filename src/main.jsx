import React from "react";
import ReactDOM from "react-dom/client";
import RealEstateWebsite from "./App.jsx"; // Make sure this matches your filename
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RealEstateWebsite />
  </React.StrictMode>,
);
